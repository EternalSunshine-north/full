# 石家庄非遗 · AI 共创平台 —— 修改方案（2026-09-13）

> 本文档把你口述的 5 条要求整理成可执行的修改条目：先给「需求润色对照」，再逐条给出**现状 → 改动 → 涉及文件 → 验收标准**，最后是需要你确认的 3 个点。
> 说明：本文档只描述方案，代码尚未改动。

---

## 一、需求润色对照（原话 → 可执行描述）

| # | 你的原话（口语） | 润色后的需求描述 |
| --- | --- | --- |
| 1 | 把相关非遗那个板块儿的上面的这部分那一块大字和小字儿删掉，从「被更多人看见的中国手艺」开始；下面那个「会动的视觉效果」那一串小字儿删掉 | 相关非遗页：删除页头的大标题与说明文字，让页面从「全国著名非遗文化 / 被更多人看见的中国手艺」区块开始；并删除该区块标题下方的说明小字 |
| 2 | 首页底下那个素材来源那一串删掉 | 删除页脚「素材来源：用户提供的《非遗项目.zip》……」这段说明文字 |
| 3 | 把 AI 创作部分的问答模式工作流和视频模式工作流分开，就相当于非遗列表里「非遗项目 / AI 共创作品」那两个一样；把最下边的 AI 共创举例部分移到视频生成模块工作流的上面 | AI 创作页改为**两个 Tab 切换**（问答模式工作流 / 视频生成模块工作流），一次只显示一个；并把「推荐非遗作品（AI 共创举例）」移到视频生成模块内容的**上方** |
| 4 | 最右边那个个人中心，点击之后弹出的个人中心 / 我的收藏 / 退出登录，要展示在图标下面 | 顶部导航头像的下拉菜单，锚定在**头像图标正下方**展开 |
| 5 | AI 右边那个小按钮（悬浮球）让它固定不动，或者一直点着它才能动 | 问答悬浮球默认**固定位置**；若需移动，则改为**长按后才能拖动**，单击仍只负责展开/收起 |

---

## 二、逐条修改方案

### 1. 相关非遗页：精简顶部标题区

**现状**

- 页头区块（`src/app/news/page.tsx` 第 41–52 行）：
  - 小标签 `相关非遗`
  - 大标题 `相关非遗 · 从石家庄看全国`
  - 说明段「这里既有全国范围内最被大众熟知的非遗项目……」
- 「全国著名非遗文化」区块（第 54–72 行）：
  - 小标签 `全国著名非遗文化`
  - 大标题 `被更多人看见的 中国手艺`
  - 说明段「下面每张卡片都用代码绘制了会动的「动态视觉」……放入 public/national/ 即可」← 这串小字要删

**改动**

1. 删除整个页头区块（小标签 + 大标题 + 说明段）。
2. 删除「全国著名非遗文化」区块标题下方的说明段。
3. 保留：`全国著名非遗文化` 小标签、`被更多人看见的中国手艺` 大标题、右侧「看石家庄本地非遗」按钮、下方 8 张全国非遗卡片。

**涉及文件**：`src/app/news/page.tsx`

**验收标准**：打开 `/news`，页面第一屏顶部直接是「全国著名非遗文化 / 被更多人看见的中国手艺」，其下没有说明小字。

**待确认**：小标签「全国著名非遗文化」是保留（页面从这一行开始），还是一并删除、让「被更多人看见的中国手艺」成为第一行？

---

### 2. 页脚：删除素材来源说明

**现状**：`src/components/SiteFooter.tsx` 第 46–48 行

> 素材来源：用户提供的《非遗项目.zip》（实拍图片、影像与 AI 共创图）。页面文字为素材文档与公开资料整理，若与官方名录不一致，以官方公布为准。

**改动**：删除该段文字，页脚品牌区的简介（「以数字化档案整理石家庄非物质文化遗产……」）保留。

**涉及文件**：`src/components/SiteFooter.tsx`

**验收标准**：任意页面底部不再出现「素材来源……」字样。

**待确认**：页脚是全站组件，删除后**所有页面**都不再显示；如果你只想让它在首页隐藏、其他页面保留，请说明，我改成按页面判断（备选方案 B）。

---

### 3. AI 创作页：两个模块拆成 Tab + 推荐作品上移

**现状**

- `src/app/create/page.tsx` 目前是**上下堆叠**的长页面：
  - 第 93–105 行：模块一「问答模式工作流」→ 问答界面
  - 第 107 行起：模块二「视频生成模块工作流」→ 视频生成表单 + 生成结果
  - 第 272 行起：模块二内部最下方是「推荐非遗作品（AI 共创举例）」画廊

**改动**

1. **改为 Tab 切换**，样式与交互对齐非遗列表页那两个胶囊按钮（`src/app/heritage/page.tsx` 的「非遗项目 / AI 共创作品」）：
   - Tab 1：**【问答模式工作流】** → 显示问答界面
   - Tab 2：**【视频生成模块工作流】** → 显示视频生成模块
2. 去掉「模块一 / 模块二」字样，模块名由 Tab 承担。
3. 支持地址直达与刷新保持：`/create?tab=qa`、`/create?tab=video`；默认进入「问答模式工作流」。
4. **推荐非遗作品（AI 共创举例）上移**：从页面底部移入「视频生成模块工作流」Tab 内容的**最上方**，顺序变为「推荐作品 → 视频生成表单 → 生成结果」。

**涉及文件**：`src/app/create/page.tsx`（必要时把两个模块各拆成一个子组件，便于 Tab 内渲染）

**验收标准**

- `/create` 顶部只有两个 Tab，页面不再出现两段模块标题同时铺开；
- 切到「视频生成模块工作流」时，最上方是推荐非遗作品，其下才是视频生成表单与结果区；
- 切到「问答模式工作流」时只显示问答界面；
- 刷新页面后仍停留在当前 Tab。

**待确认**：推荐非遗作品是放在**视频 Tab 内最上方**（我的理解），还是仍保留在两个 Tab 之外的页面底部？另外首页统计卡片里的「AI 共创作品」入口目前跳到 `/heritage?tab=works`，需要改成 `/create?tab=video` 吗？

---

### 4. 顶部个人中心下拉菜单：锚定在头像正下方

**现状**：`src/components/SiteHeader.tsx` 第 89–127 行

- 菜单容器：`absolute right-0 mt-2 w-44`，锚点是包裹「头像 + 昵称」的 `div`，因此菜单右边缘对齐的是**整个按钮**（含昵称文字）的右侧，视觉上会偏右，不像"挂在头像下面"。

**改动**

1. 把定位锚点从「按钮整体」改为「头像图标」：
   - 方案 A：菜单右边缘与**头像右边缘**对齐；
   - 方案 B（推荐）：菜单**水平居中于头像图标正下方**，垂直间距 8–10px。
2. 菜单宽度统一为 176–200px，并在小屏做视口收敛，避免贴到屏幕外。
3. 顺手补上「点击菜单外部自动关闭」与 `Esc` 关闭（目前只有再次点击按钮才会关闭）。

**涉及文件**：`src/components/SiteHeader.tsx`

**验收标准**：点击右上角头像，菜单出现在头像正下方（右对齐或居中，按确认结果），不会偏到按钮右侧或超出屏幕。

---

### 5. 悬浮球：固定不动，或「长按才能拖」

**现状**：`src/components/QAWidget.tsx`

- 鼠标/手指按下后移动超过 4px 即视为拖动，位置写入 `localStorage`（键名 `sjz-ich-qa-ball`）；
- 拖动结束若未移动，则视为单击 → 展开 / 收起浮窗。
- 问题：轻微手抖就会把球拖走，位置不再固定。

**改动（二选一，我建议先做 A）**

- **方案 A｜固定不动**：删除拖拽逻辑，悬浮球固定右下角（`right: 28px; bottom: 96px`），只保留「单击展开 / 收起」；同时清除历史拖拽记录，避免旧位置仍生效。
- **方案 B｜长按才能拖**：单击仍为展开 / 收起；**按住 400ms** 才进入可拖动状态（进入拖动时球体轻微放大、光标变为移动样式作为反馈），松手后保存新位置。

**涉及文件**：`src/components/QAWidget.tsx`

**验收标准**：单击只负责展开 / 收起，不做长按（或未启用拖动）时悬浮球位置永不改变；浮窗仍按 3:4 竖版展示，且不影响页面跳转。

---

## 三、实施顺序与预估工作量

| 顺序 | 内容 | 预估 | 风险 |
| --- | --- | --- | --- |
| 1 | 第 1、2 条（删除两处文字） | 10 分钟 | 低 |
| 2 | 第 5 条（悬浮球固定 / 长按拖动） | 20 分钟 | 低 |
| 3 | 第 4 条（菜单位置与关闭逻辑） | 20 分钟 | 低（需小屏复核） |
| 4 | 第 3 条（Tab 改造 + 推荐作品上移） | 40–60 分钟 | 中（页面结构改动最大） |

合计约 1.5–2 小时。改完需要重新构建：**先 `Ctrl + C` 停掉服务**，再执行 `npm run build`、`npm start`（两种模式不要同时运行）。

---

## 四、本次不改动的部分

- 配色（米咖暖调）、数据（10 个非遗项目 / 18 个地图点位 / 政策新闻）、其他页面逻辑保持不变；
- 已删除的「工作流审阅」页面（原 `/workflow`）不恢复；
- 视频生成模块仍需用户填写视频模型与 Key 才能真正出片。

---

## 五、需要你确认的 3 个点

1. **相关非遗页**：小标签「全国著名非遗文化」保留，还是一并删除（让「被更多人看见的中国手艺」成为第一行）？
2. **页脚素材来源**：确认**全站删除**，还是只让首页不显示？
3. **AI 创作页**：推荐非遗作品放在**视频 Tab 内最上方**是否正确？个人中心菜单要**右对齐头像**还是**居中在头像下方**？

确认后我按此方案逐条实施，并在改完后给出改动清单与逐条验收结果。

---

## 六、实施结果（已完成，2026-09-13）

按本方案逐条实施完毕，浏览器自动化验收结果如下：

| 条目 | 结果 |
| --- | --- |
| 1 相关非遗页精简 | 页头区块（`相关非遗 · 从石家庄看全国` 及说明）已删除；页面首个标题为「被更多人看见的 中国手艺」；标题下方技术说明小字已删除（保留小标签「全国著名非遗文化」） |
| 2 页脚素材来源 | 已删除，全站页脚不再出现「素材来源……」 |
| 3 AI 创作 Tab 化 | 顶部改为两个 Tab，默认「问答模式工作流」；点击「视频生成模块工作流」后地址变为 `/create?tab=video`，且**推荐非遗作品位于视频生成模块上方**；「模块一 / 模块二」字样已移除 |
| 4 个人中心菜单 | 菜单水平中心与头像中心偏差 1px、位于头像正下方、3 个菜单项、未超出视口；新增点击外部与 Esc 关闭 |
| 5 悬浮球固定 | 拖拽逻辑已移除（模拟拖拽后位置不变），固定右下角（距右 28px / 距底 96px）；单击仍可展开 3:4 竖版浮窗（实测 402×536，比例 0.750）；历史拖拽位置已清理 |

**实施中的两点说明**

1. 相关非遗页保留了小标签「全国著名非遗文化」（页面从该标签 + 「被更多人看见的中国手艺」开始）。若要去掉小标签，让大标题成为第一行，改一处即可。
2. 底部「从石家庄出发，汇入全国非遗版图」总结段落中仍有一句提到「动态视觉」，属于叙述性文案（不是那处技术说明小字），按原文保留；如需一并去掉请说明。
