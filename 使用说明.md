# 使用说明 · 石家庄非遗文化 AI 艺术共创平台

> 项目路径：`D:\AI\wangye\codex\aaa`
> 技术栈：Next.js（App Router）+ TypeScript + SCSS + TailwindCSS v4 + ECharts / Leaflet + DeepSeek

---

## 0. 一分钟上手

```powershell
cd D:\AI\wangye\codex\aaa
npm start
```

然后浏览器打开 **http://localhost:3000**（强刷 Ctrl + Shift + R 可避免旧缓存）。

如果提示 `Could not find a production build`，说明还没构建，先执行：

```powershell
npm run build
npm start
```

---

## 1. 环境要求

| 项目 | 要求 |
| --- | --- |
| Node.js | 18.18 以上（本项目在 Node 24.14 上验证通过） |
| npm | 9 以上 |
| 浏览器 | Chrome / Edge 等现代浏览器 |
| 依赖 | 已安装（`node_modules` 已存在）；换电脑需先 `npm install` |

若 `npm install` 报缓存目录无权限（EPERM），改用项目内缓存：

```powershell
npm install --cache ./.npm-cache
```

---

## 2. 启动与停止（两种模式，**不要同时开**）

### 生产模式（推荐，启动快、页面已优化）

```powershell
npm run build     # 改了代码/数据后需要重新构建
npm start         # 访问 http://localhost:3000
```

### 开发模式（改代码即时生效，适合边改边看）

```powershell
npm run dev       # 访问 http://localhost:3000
```

**停止服务**：在运行服务的终端窗口按 `Ctrl + C`。

> ⚠️ 两种模式共用同一个 `.next` 构建目录。
> **不要同时运行 `npm run dev` 和 `npm start`，也不要在服务运行时执行 `npm run build`**，
> 否则构建产物会被互相覆盖，典型表现就是页面「没有样式」。切换模式前先 `Ctrl + C` 停掉当前服务。

### 换端口 / 局域网访问

```powershell
npx next start -p 3001        # 生产模式换端口
npm run dev -- -p 3001        # 开发模式换端口
```

服务启动时会打印 `Network: http://192.168.x.x:3000`，同一局域网的手机、平板可用该地址访问。

---

## 3. 页面导览

顶部导航共 6 个一级入口：

| 入口 | 地址 | 游客可访问 | 主要功能 |
| --- | --- | --- | --- |
| 首页 | `/` | ✅ | 平台简介（文字在上）、**Banner 轮播图**、可点击的统计卡片、**十大非遗竖排列表**、文化精选、用户评价 |
| AI 创作 | `/create` | ❌ 需登录 | 两个独立模块：**【问答模式工作流】**（直接给最终答案）与**【视频生成模块工作流】**（含推荐非遗作品） |
| 文化地图 | `/map` | ✅ | **离线行政区划图**（本地 GeoJSON，断网可用）、**实心小圆点**点位、资源统计图表、点位侧滑详情 |
| 非遗列表 | `/heritage` | ✅ | **非遗项目 / AI 共创作品 两个分类**、关键词检索、分类/地域/等级筛选、只看收藏、卡片悬停「详细」 |
| 相关非遗 | `/news` | ✅ | 全国著名非遗文化（动态视觉）、非遗影像（视频墙）、政策与新闻动态、从石家庄到全国的总结（中国地图） |
| 个人中心 | `/me` | ❌ 需登录 | 用户信息、我的收藏、浏览历史 |

### 几个容易忽略的交互

- **首页首屏**：文字内容在上方，**Banner 轮播图在其下方**，每 4.5 秒自动切换（鼠标悬停暂停，支持左右箭头 / 指示点 / 进度条），**每一张都可以点击**跳转到对应非遗详情页；统计卡片（收录非遗项目 / 文化地图点位 / 覆盖县市区 / AI 共创作品）也都可以点击跳转。
- **十大非遗**：改为**竖排纵向列表**（带序号 01–10、缩略图、等级/分类/地域标签、摘要、收藏与「查看 →」），自上而下依次浏览。
- **AI 创作页**：顶部是**两个标签页**——【问答模式工作流】与【视频生成模块工作流】，一次只显示一个模块；视频标签页里**最上方是推荐非遗作品**，其下才是视频生成表单。支持 `/create?tab=qa`、`/create?tab=video` 直达，切换后地址栏会同步。
- **相关非遗页**：页头大标题与说明已按需求删除，页面从「全国著名非遗文化 / 被更多人看见的中国手艺」开始，该标题下方原有的技术说明小字也已移除。
- **顶部个人中心**：点击右上角头像，菜单出现在**头像正下方**（水平居中于图标），点击菜单外部或按 `Esc` 可关闭。
- **问答悬浮球**：固定在右下角（距右 28px、距底 96px），**不可拖动**，单击展开/收起 3:4 竖版浮窗。
- **非遗卡片**：鼠标移到卡片上，**左上角会渐入「详细」按钮**，点它进详情页；卡片右上角 ☆ 可收藏。
- **非遗列表**：顶部可切换「非遗项目」和「AI 共创作品」，AI 共创作品可追溯到所属项目；也可以用 `/heritage?tab=works` 直接打开作品列表。
- **详情页**：包含项目介绍、为什么列入非遗、存在意义、传承人/传承方式、图片档案、影像资料、AI 共创作品。
- **文化地图**：只保留**离线行政区划图**（已移除在线路网地图相关组件与功能）——区县填色深浅 = 该区县资源数量，**暖墨色实心小圆点** = 非遗点位（比原来更小），点击点位右侧滑出详情（手机端为底部抽屉）。
- **相关非遗页**：顶部是全国著名非遗（打铁花、苏绣、景德镇制瓷、昆曲、皮影戏、川剧变脸、二十四节气、中国剪纸）的**动态视觉卡片**；中间是**非遗影像墙**；下面是政策与新闻；最底部是「从石家庄到全国」的总结与中国地图。
- **AI 创作**：左侧是对话历史（可新建、删除、本地保存），中间是对话气泡，底部输入框（Enter 发送，Shift + Enter 换行）。
- **桌面悬浮球（全站）**：右下角有一个可拖动的「非遗问答」悬浮球，**点击展开 3:4 竖版问答浮窗**，展开/收起都不会切换页面路由（首页仍停在首页）。浮窗内可切换回答风格、发起多轮对话、查看来源标签；「完整版 ↗」才跳转到 `/create`。悬浮球在 `/create`（页面本身已有完整问答）和 `/login` 不显示。

---

## 4. 登录与权限

- **演示登录**：账号随意填（手机号 / 邮箱 / 用户名均可），密码 ≥ 4 位，点「登录并继续」即可。
  登录态保存在浏览器 localStorage（键名 `sjz-ich-user`），**不会上传服务器**。
- **需要登录的功能**：AI 创作、收藏、个人中心。未登录访问会自动跳转到 `/login?next=原地址`，登录后自动回跳。
- **退出登录**：右上角头像 → 退出登录；或清空浏览器 localStorage。
- **收藏 / 浏览历史**：分别存在 `sjz-ich-collected` 与 `sjz-ich-history`（最多保留 40 条历史）。

> 接真实账号体系时，替换 `src/lib/auth.tsx` 与 `/api` 接口即可。

---

## 5. AI 智能体（DeepSeek）

### 默认状态

本项目已在 `.env.local` 里配置 DeepSeek Key，**问答与视频分镜都由服务端自动调用**，前端不需要填任何接口或密钥。
如果 Key 未配置或调用失败，会自动回退到**本地知识库模式**（用平台内置的 18 个非遗项目 + 地图点位 + 政策新闻资料回答），
适合演示与离线环境。

### 接入真实 DeepSeek

1. 复制环境变量模板：

   ```powershell
   copy .env.local.example .env.local
   ```

2. 编辑 `.env.local`，填入自己的 Key（申请地址 https://platform.deepseek.com/）：

   ```
   DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
   DEEPSEEK_BASE_URL=https://api.deepseek.com
   DEEPSEEK_MODEL=deepseek-chat
   ```

3. 重启服务（先 `Ctrl + C`，再 `npm run build && npm start` 或 `npm run dev`）。

### 检查是否接入成功

浏览器打开 **http://localhost:3000/api/chat**，返回示例：

```json
{ "agent": "deepseek", "configured": true, "model": "deepseek-chat", "endpoint": "/api/chat" }
```

`configured: true` 表示 Key 已生效；对话页面右上角徽标也会从「本地知识库」变成「DeepSeek」。

**安全说明**：API Key 只在服务端使用（`src/app/api/chat/route.ts` 转发），不会出现在浏览器代码里。

### 换成其他智能体

- 前端接入位：`src/components/AIAgentSlot.tsx` 中的 `<div id="ai-agent-container">` 与常量 `AGENT_ENDPOINT`
- 服务端转发：`src/app/api/chat/route.ts`（统一 SSE 协议：`data: {"delta":"..."}`，以 `data: [DONE]` 结束）
- 换扣子 / Dify / 百炼 / 自建服务时，只需改这两处的地址与请求体字段，页面其余部分不用动。

### 工作流（后台定义保留，审阅 UI 已下线）

> 按最新改造要求，「工作流审阅 / 调试」页面（原 `/workflow`）与 AI 创作页里的调试面板**已全部移除**；
> 工作流定义仍保留在 `src/lib/workflow/`（供接入与接口调用），AI 创作页现在只有两个面向用户的模块。

两条工作流的步骤与状态：

| 工作流 | 步骤 | 已实现 | 待接入 |
| --- | --- | --- | --- |
| 文生图工作流 `text-to-image` | 8 | 7 步（输入校验 / 需求解析 / 非遗事实增强 / 参考图检索 / 提示词编译 / 质检 / 归档） | 第 6 步「图像生成」按用户所选模型调用 |
| 问答模式工作流 `qa-rag` | 8 | 8 步（规范化 / 意图路由 / 混合检索 / 提示词组装 / 生成 / 事实校验 / 输出后处理 / 会话记忆） | 已完成 |
| 视频生成模块工作流 | — | 提示词 + 时长/画幅/帧率 + 模型选择 → `POST /api/video` | 需用户填写视频模型与 Key |

- 命令行仍可调用工作流接口，便于联调与自动化测试：

  ```powershell
  curl http://localhost:3000/api/workflow                          # 工作流清单
  curl "http://localhost:3000/api/workflow?id=text-to-image"       # 某条工作流定义
  curl -X POST http://localhost:3000/api/workflow -H "Content-Type: application/json" -d "{\"workflowId\":\"text-to-image\",\"input\":\"把井陉拉花的拧肩翻腕动作做成海报，红金配色，竖版\"}"
  ```

- **视频生成**：`POST /api/video`，服务端自动完成「DeepSeek 分镜 → 浏览器本地合成」；若配置了 `DASHSCOPE_API_KEY`，则优先调用视频模型直接出片。可用 `GET /api/video` 查看当前生效的模型与 Key 状态。
- **图像生成**：`POST /api/image`；模型与 Key 由用户在「图像模型」模块里选择或自行填写。
- 问答链路已经接入：`generate` 节点 → `/api/chat` → DeepSeek；未配置 Key 时自动用本地知识库作答。

#### 已按确认回执接入的部分

**视频生成模块（全自动，无需用户配置模型）**：用户只写一句提示词，服务端自动完成下面两步：

| 步骤 | 由谁完成 | 说明 |
| --- | --- | --- |
| 生成分镜脚本 | 服务端自动调用 DeepSeek（`.env.local` 的 Key） | 输出片名 + 3–5 个镜头（画面描述 / 字幕 / 时长），并自动关联对应非遗项目的素材图 |
| 合成视频 | 浏览器本地合成（canvas + MediaRecorder） | 按分镜做图片推拉运镜 + 字幕 + 转场，产出可播放、可下载的 `.webm` 短视频 |

> 说明：DeepSeek 只提供文本模型、不具备视频生成能力，所以默认走「自动分镜 + 本地合成」；
> 如果以后拿到视频模型（如通义万相 · 视频），把 `DASHSCOPE_API_KEY` 写进 `.env.local`，接口会优先直接出片，前端同样不需要改。

页面上的可调项：

| 字段 | 说明 |
| --- | --- |
| 时长 / 画幅 / 帧率 | 10 / 15 / 20 秒；16:9 / 9:16 / 1:1；24 / 30 fps |
| 分镜来源 | 由平台自动生成；若未取得模型回复，会按提示词给出基础分镜，功能不中断 |
| 结果 | 页面内可直接播放，并提供「下载视频」按钮 |

**图像生成接口** `POST /api/image`：

- `provider=dashscope`（通义万相，异步任务 + 轮询）、`openai`（gpt-image）、`local-sdxl`（Stable Diffusion WebUI `/sdapi/v1/txt2img`）、`custom`（自建接口）
- `provider=jimeng`（即梦）需要企业接入签名，接口会返回明确的接入说明
- Key 优先用请求里的「用户临时 Key」，否则用服务端环境变量（`DASHSCOPE_API_KEY` / `OPENAI_API_KEY`）
- 生成的图片保存到 `public/generated/`，勾选归档后自动写入作品库

**问答链路的三个确认项**：

1. **意图路由改为 LLM 分类**：`/api/chat` 与 `/api/analyze` 走 DeepSeek 低温度 JSON 分类，未配置 Key 或超时自动退回关键词规则（返回里会标注 `engine`）。
2. **按用户存储会话 + 自动摘要**：`/api/memory`（GET / POST / DELETE），保存在 `.data/memory/<账号>.json`；超过 16 条自动摘要，只保留最近 12 条原文（有 Key 时由 DeepSeek 摘要，否则规则截断）。
3. **回答风格模板**：教学讲解 / 短视频口播 / 儿童版 / 由我询问，AI 小助手对话区顶部可直接切换，默认「由我询问」。

**作品归档** `POST /api/works`：生成的作品写入 `.data/works.json`，在「非遗列表 → AI 共创作品」顶部以「最新生成的作品」展示，含署名、授权范围与提示词，可删除。

#### 桌面悬浮球（问答工作流入口）

全站右下角有一个可拖动的「非遗问答」悬浮球，点击展开一个 **3:4 竖版浮窗**，展开与收起都**不切换页面路由**（首页仍停在首页）。

- 浮窗内容：标题栏（新对话 / 完整版 ↗ / 收起）、回答风格模板、消息气泡（带意图 / 风格 / 来源标签）、输入框（Enter 发送，Shift+Enter 换行）、记忆状态
- 与 `/create` 的完整问答**共用同一套逻辑**（`src/lib/useQAChat.ts`），因此意图路由、RAG 检索、事实校验、按用户记忆在浮窗里同样生效
- 未登录也能提问（会话只存本机 localStorage，键名 `sjz-ich-qa-ball` 存悬浮球位置）；登录后会话写入 `/api/memory` 并自动摘要
- 悬浮球在 `/create`（页面本身已有完整问答）与 `/login` 不显示，避免重复
- 尺寸策略：宽高严格 3:4（桌面默认 402×536），并会自动缩放到不出屏；悬浮球位置拖动后记住

```
src/lib/useQAChat.ts             问答工作流客户端状态机（会话 / 风格 / 流式 / 记忆）
src/components/QAWidget.tsx       全站桌面悬浮球 + 3:4 竖版浮窗
src/components/AIAgentSlot.tsx    /create 上的完整问答界面（与悬浮球共用状态机）
src/components/RichText.tsx       回答富文本渲染（共用）
```

---

## 6. 内容维护（不用改页面代码）

| 想改什么 | 改哪个文件 |
| --- | --- |
| 非遗项目资料（介绍、历史、传承、标签、等级、坐标） | `src/lib/ich.ts` |
| 文化地图点位与数量（行政区划归属见 `districtOf()`） | `src/lib/mapData.ts` |
| 相关非遗页顶部「全国著名非遗」卡片（含动态视觉效果、可选图片） | `src/lib/nationalIch.ts` |
| 相关非遗页「非遗影像」视频墙（本地视频 / 外链嵌入 / 站外检索） | `src/lib/videos.ts` |
| 政策 / 新闻 / 倡议（相关非遗页下半部分） | `src/lib/newsData.ts` |
| 首页推荐作品、文化精选、用户评价、平台统计 | `src/lib/works.ts` |
| 图片、视频文件 | `public/ich/<项目 slug>/` |
| 地图用到的行政区划数据 | `public/geo/shijiazhuang.json`（石家庄）、`public/geo/china.json`（全国）、`public/geo/hebei.json`（河北） |
| 素材包重新整理脚本 | `scripts/prepare-assets.ps1`（执行 `npm run assets`） |

### 三个「接入位」怎么用

| 想接入什么 | 怎么改 |
| --- | --- |
| 全国著名非遗的实拍图片 | 图片放到 `public/national/`（自行新建），在 `src/lib/nationalIch.ts` 对应项目的 `image` 字段填 `/national/xxx.jpg`；不填就用内置的动态视觉 |
| 打铁花 / 李子柒等第三方视频 | 在 `src/lib/videos.ts` 对应条目填 `embedUrl`（B 站 / 抖音 播放器 iframe 地址）或 `src`（已授权的本地视频路径）；只填 `searchUrl` 时卡片会提供「站外查找」按钮 |
| 地图新增点位 | 在 `src/lib/mapData.ts` 增加一条，包含 `name / location / coordinates / count / description / image`；`location` 前缀要与 `public/geo/shijiazhuang.json` 里的区县名一致（如「正定县」「井陉县」），市区点位用「石家庄市区」会自动归入长安区 |

### 图片与视频的命名约定

每个非遗项目一个文件夹（slug 与 `src/lib/ich.ts` 里的 `id` 一致）：

```
public/ich/jingxing-lahua/
├─ cover.jpg     # 封面（卡片、详情页头图）
├─ g1.jpg …      # 图集（详情页「图片档案」）
├─ ai1.jpg …     # AI 共创图（详情页「AI 共创作品」）
└─ video.mp4     # 影像资料
```

### 新增一个非遗项目（4 步）

1. 新建目录 `public/ich/<新slug>/`，放入 `cover.jpg`、`g1.jpg`、`ai1.jpg`、`video.mp4` 等文件；
2. 在 `src/lib/ich.ts` 的 `ichList` 数组里复制一个对象，改成新项目的 `id`（必须是 slug 同名）、名称、分类、地域、坐标、介绍等字段；
3. 如需在地图上显示，在 `src/lib/mapData.ts` 增加一个点位（`name`、`location`、`coordinates`、`count`、`description`、`image`，并可用 `ichId` 关联详情页）；
4. 重新构建：`npm run build`，再 `npm start`（列表页、首页十大非遗、详情页、地图会自动同步）。

> 原始素材若还在压缩包里，可解压到 `素材源/非遗项目/` 后执行 `npm run assets`，脚本会按约定批量复制并重命名。

---

### 本次新增的非遗项目（8 项，共 18 项）

平台首批基于素材包收录 10 项非遗，本次按公开资料补充 8 项，非遗列表与相关入口会自动同步（数量、筛选、地图点位均从数据读取）：

| 新增项目 | 类别 | 地域 | 名录级别 |
| --- | --- | --- | --- |
| 藁城宫灯 | 传统技艺 | 藁城区 | 省级 |
| 井陉晋剧 | 传统戏剧 | 井陉县 | 国家级（晋剧扩展项目） |
| 赵县范庄龙牌会 | 民俗 | 赵县 | 国家级 |
| 辛集皮贴画 | 传统美术 | 辛集市 | 省级 |
| 平山民歌 | 传统音乐 | 平山县 | 省级 |
| 深泽坠子戏 | 传统戏剧 | 深泽县 | 省级 |
| 新乐伏羲祭典 | 民俗 | 新乐市 | 省级 |
| 赞皇铁龙灯 | 传统舞蹈 | 赞皇县 | 省级 |

说明：这 8 项素材包内没有对应图片，封面暂用同类非遗场景示意图，详情页会显示「图片说明：暂无实拍素材……」；
名录等级与批次依据公开资料整理，**页面已标注「以官方公布名录为准」**，正式发布前建议核对官方名录后再定稿。
另外，新增项目的详情页在「图片档案 / 影像资料 / AI 共创作品」为空时会给出提示（例如「欢迎成为第一个创作者」），并保留 AI 共创入口。

### 文化地图点位详情（已完善）

地图上共 19 个点位，其中 **18 个已建立完整项目档案**（集合点位除外），点位数据在 `src/lib/mapData.ts` 里通过 `ichId` 关联到 `ich.ts` 的项目：

- 点击有档案的点位，侧滑面板会展示：**项目标签 → 项目简介 → 核心看点（3 条）→ 项目图片（最多 3 张，含素材说明）→ 名录级别等关键信息 → 点位说明**，底部提供「查看完整档案」「用 AI 讲讲它」两个入口；
- 只有「正定古城庙会花会」这类**集聚点位**没有独立档案，面板会改为列出**该点位关联的非遗项目**（可直接点进各自详情页），不再出现「尚未建立详情页」的空白提示；
- 想给某个点位补充档案：在 `ich.ts` 里新增项目，再把 `mapData.ts` 中对应点位的 `ichId` 指向它即可，面板内容会自动带出，无需改页面代码。

---

## 7. 常见问题

**① 页面能打开但没有样式**

最常见原因：开发模式和生产模式同时运行，或服务运行期间执行了构建，导致 CSS 产物被覆盖。

处理步骤（在项目目录依次执行）：

```powershell
# 1) 停掉所有服务：在对应终端按 Ctrl + C；或强制结束占用 3000 端口的进程
netstat -ano | findstr :3000
taskkill /PID <上面查到的PID> /F

# 2) 清掉被污染的构建目录
Remove-Item .next -Recurse -Force

# 3) 重新构建并启动
npm run build
npm start
```

最后在浏览器按 **Ctrl + Shift + R** 强刷（旧的无样式响应可能被缓存）。

**② `npm start` 报 Could not find a production build**

还没构建，或 `.next` 被清掉了：执行 `npm run build` 后再 `npm start`。

**③ 报 EADDRINUSE: address already in use :::3000**

端口被占用（多半是已有一个服务在跑）。用上面的 `netstat` + `taskkill` 结束旧进程，或换端口 `npx next start -p 3001`。

**④ 构建报 Type error: File '.next/types/...' not found**

增量缓存失效，删掉缓存文件后重新构建：

```powershell
Remove-Item tsconfig.tsbuildinfo -Force -ErrorAction SilentlyContinue
npm run build
```

**⑤ AI 回答前面写着「本地知识库模式」**

说明没有配置 `DEEPSEEK_API_KEY`，或调用 DeepSeek 失败（网络/额度/Key 错误）。按第 5 节配置后重启即可；本地模式本身就是兜底方案，功能可以正常演示。

**⑥ 视频加载慢或黑屏**

素材包里的视频共约 390MB（最大 127MB）。页面已设置 `preload="none"`，只在点击播放时加载。
正式部署建议把视频压缩转码或放到对象存储/CDN 上。

**⑦ 文化地图显示不出来 / 一片空白**

地图为**离线行政区划图**，数据来自 `public/geo/*.json`，不依赖外网（在线路网地图已按要求移除）。
若显示「正在加载地图数据…」不消失或提示读取失败，检查 `public/geo/shijiazhuang.json` 是否存在，然后重新 `npm run build && npm start`。

**⑧ 图片不显示**

检查三点：文件名大小写是否与代码一致、路径是否以 `/ich/` 开头、文件是否确实在 `public/ich/` 下（`public` 目录下的文件直接通过 `/文件名` 访问）。

---

## 8. 构建与部署

```powershell
npm run build     # 生成 .next 生产构建
npm start         # 以 Node 服务方式运行，默认 3000 端口
```

- 部署到服务器：把整个项目目录（至少包含 `src`、`public`、`package.json`、`node_modules` 或重新 `npm install`）拷过去，在服务器上 `npm run build && npm start`；用 pm2 / systemd 常驻时注意以生产模式启动。
- 注意体积：`public/ich` 约 457MB（103 个文件，含 11 段视频）。部署前建议压缩图片、转码视频或改走 CDN。
- 若部署到 Vercel 等平台，注意大文件与时长限制，建议把 `public/ich` 的媒体改为外部存储。

---

## 9. 目录速查

```
src/app/           页面路由（首页 / create / map / heritage / news=相关非遗 / me / login）
src/app/api/chat/  DeepSeek 转发接口
src/components/    AIAgentSlot（智能体接入位）、HeroShowcase（首屏轮播）、GeoMap（离线区划图）、
                   AnimatedArt（全国非遗动态视觉）、VideoWall（影像墙）、
                   HeritageCard、EChart、Particles、Reveal、CountUp、Protected、CollectButton 等
src/lib/           ich.ts（项目数据）、mapData.ts（地图点位）、nationalIch.ts（全国著名非遗）、
                   videos.ts（视频墙）、newsData.ts（政策新闻）、works.ts、knowledge.ts、
                   auth.tsx、store.ts、chatClient.ts
src/styles/        tailwind.css（Tailwind 入口 + 主题令牌）、globals.scss、_tokens.scss
public/ich/        素材（图片 / 视频），按项目 slug 分目录
public/geo/        离线地图 GeoJSON（石家庄 / 河北 / 全国）
scripts/           prepare-assets.ps1（素材整理脚本）
素材源/            原始素材解压件（不参与构建，已加入 .gitignore）
```

更完整的技术说明、数据结构与需求对照表见 [`README.md`](README.md)。

---

## 10. 已知限制

1. 登录为**前端演示实现**，无后端校验，仅用于区分「游客 / 登录用户」两种权限。
2. 相关非遗页的政策 / 新闻、用户评价，以及文化地图中未建立详情页的点位为**示例数据**；
   第三方博主视频（如打铁花、李子柒）受版权限制未内置，提供外链入口与接入位。
3. 非遗项目的名录等级与批次依据素材包文件夹标注与文档内容整理，**若与官方最新公布名录不一致，以官方公布为准**（修改 `src/lib/ich.ts` 即可）。
4. 收藏与浏览历史保存在本地浏览器，换浏览器或清缓存后会丢失。
5. AI 生成内容仅作创意参考，涉及纹样、脸谱、服饰等文化细节时请以传承人解释与官方资料为准。

---

## 11. 视觉主题（米色暖调 · 非遗风格）

全站主题已按参考图改造为**米咖色宣纸底 + 暖沙金渐变 + 朱红/珊瑚点缀 + 墨色线条**，并配了会动的背景。
在参考图色板基础上**整体压深约 8%**，并让 R-G 差值略增（更偏红褐、少一点黄），得到更沉稳的「米咖」调。

### 参考图色板（从 4.zip 的 4.jpg 提取）

| 用途 | 取值 |
| --- | --- |
| 参考图原始色 | `#FCFCFC` `#FCFCE4` `#E4CCB4` `#FCE4CC` `#F09070` `#D0D0B0` |
| 底色（宣纸 · 压深后） | `#FBF6EC` `#F9F2E6` `#F4EADB` `#EDE0CE` |
| 中间调（暖沙/米咖） | `#E4D4BE` `#D9C5AA` `#C9B092` `#B5987A` |
| 主体（朱红/珊瑚） | `#E08D70` `#D0684C` `#B94A33` `#97392A` |
| 点缀（金/琥珀） | `#EDD9B0` `#DFB671` `#C89241` `#A9762A` |
| 辅助（橄榄/青绿） | `#AABC85` `#8B9D5B` `#6E7E42` |
| 文字（暖墨） | `#33241A` `#4E3A29` `#6F5843` |

实测：压深后首页平均色 `R219 G204 B185`（R-G=16、G-B=18），暖色像素占比 **99.4%**；
参考图为 `R242 G229 B214`（R-G=13、G-B=15）—— 色相一致，整体更深、更偏米咖。

### 怎么实现的（改色只需动两个文件）

| 文件 | 作用 |
| --- | --- |
| `src/styles/tailwind.css` | `@theme` 设计令牌。**原本的 ink-\* 深色阶整体映射到米色阶**，所以全站 `bg-ink-*` / `from-ink-*` 一起变宣纸色；gold / cinnabar / jade / neon 也换成暖色板 |
| `src/styles/_tokens.scss` | SCSS 变量（`globals.scss` 用） |
| `src/styles/globals.scss` | 宣纸卡片 `.glass`（含四角剪纸角花）、朱红印章按钮、暖色表单、暖色地图控件，以及下面这些新增的装饰与动效 |

深色主题遗留的 `text-white/xx`、`bg-white/xx`、`border-white/xx` 由 `globals.scss` 末尾一段**批量重映射**成暖墨/暖沙色，不需要逐页改：

- 普通位置 → 暖墨文字、暖沙底、暖褐描边；
- **压在照片上的文字** → 加类名 `.on-photo` 即保持米白（例如卡片图片上的等级标签、标题）；
- 代码 / JSON 展示块 → `.code-block`（暖褐底 + 米色字）。

### 非遗动态背景（全站）

组件：`src/components/IchBackdrop.tsx`，挂在 `layout.tsx` 上，`z-index: -1` 铺在最底层，不影响任何交互。四层叠加：

1. **暖色光晕漂移** —— 金/珊瑚/橄榄四团光晕 46 秒缓慢移动（`warmDrift`）
2. **宣纸受光** —— 一条高光带 30 秒缓慢扫过（`paperSheen`，soft-light 混合）
3. **纸纤维颗粒** —— 噪点纹理缓慢位移（`grainShift`）
4. **剪纸团花 / 云纹 / 缠枝纹 / 印章纹** —— 四个大幅 SVG 纹样漂浮旋转（`motifFloat`、`sealPulse`）

另外还有一层 canvas **金粉纸屑粒子**（金/朱红/橄榄/蜜色），会随鼠标轻微避让；开启系统「减弱动态效果」时自动停止动画。

### 非遗风格小装饰

| 类名 | 位置 | 效果 |
| --- | --- | --- |
| `.glass` 背景 | 所有卡片 | 四角自动带剪纸角花 |
| `.section-title::after` | 所有区块标题 | 回纹（雷纹）下划线 |
| `.seal` | 首页首屏、页脚 | 朱红方印 |
| `.fret-divider` | 页脚顶部 | 回纹分隔线 |
| `.pattern-ornament` / `.pattern-cloud` | 区块背景 | 团花剪纸纹、云纹（改为朱红/金） |
| `.art-visual` | 相关非遗页卡片、影像墙 | 十种「会动」的非遗视觉，底色统一改为暖褐 |

地图与图表也一并换成暖色：离线行政区划图用米色→暖沙棕填色，点位为暖墨色实心小圆点，ECharts 提示框/坐标轴/柱状渐变全部改为暖调。

### 想换配色

只改 `src/styles/tailwind.css` 的 `@theme` 与 `src/styles/_tokens.scss` 里的色值即可全站生效，然后 `npm run build`（记得先停掉正在运行的服务）。
